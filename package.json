{
  "name": "@unbird/utils",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=22.17.0"
  },
  "scripts": {
    "build:packages": "pnpm -rF=\"./packages/*\" --parallel build",
    "lint": "eslint --cache .",
    "lint:fix": "eslint --cache . --fix",
    "typecheck": "pnpm -r --parallel typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@unbird/eslint-config": "workspace:*",
    "eslint": "^9.33.0",
    "typescript": "^5.9.2",
    "unplugin-unused": "^0.5.2",
    "vitest": "^3.2.4"
  }
}
