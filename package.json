{
  "name": "@tg-search/utils",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=22.17.0"
  },
  "scripts": {
    "build:packages": "pnpm -rF=\"./packages/*\" --parallel build",
    "lint": "eslint --cache .",
    "lint:fix": "eslint --cache . --fix",
    "typecheck": "pnpm -r --parallel typecheck"
  },
  "devDependencies": {
    "@tg-search/eslint-config": "workspace:*",
    "@types/node": "^24.0.12",
    "eslint": "^9.30.1",
    "typescript": "^5.8.3"
  }
}
